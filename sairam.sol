#***Smart Contract Templat Generated from BPMN file(XML)***

#====Precausions to avoid common vulnerabilites:====

#=> Use transfer() instead of send() for money transfer operations.
#=> Use msg.sender instead of tx.origin for authentication
#=> Specify visibility (Public/private) explicitly.
#=> Use datatype uint256 in case of loop iterations >256 (instead of uint8, default type). 
#=> use bytes[] instead of byte[] for lower gas consumption.
#=> Avoid conditional statements (if/for/while) with function call inside the condition.

pragma  solidity  0.4.24;
import safeMath; #To avoid Integer overflow and underflow vulnerabilities.
contract  SolidityContract {

function  Credit Amount( Yes') payable  public return( ) {'
	...
	...
}
function  Send Money( Balance, Amount, Rama') payable  public return( ) {'
	...
	...
}
